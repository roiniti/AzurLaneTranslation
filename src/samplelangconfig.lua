---This an example file, do not load



--Register the lang code
FMLTranslator.LANGUAGE = "code" --the code must be the same as the name of the folder

--Register the language
table.insert(FMLTranslator.languages, {
    name = "Name",
    code = FMLTranslator.LANGUAGE
})
